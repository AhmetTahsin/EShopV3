@model List<ResultSpecialOfferDto>
@{
    int count = 0;
}

<div class="col-lg-4">
    @foreach(var item in Model)
    {
        count++;
        <div class="product-offer mb-30" style="height: 200px;">
            <img class="img-fluid" src="@item.ImageUrl" alt="">
            <div class="offer-text">
                <h6 class="text-white text-uppercase">@item.Title</h6>
                <h3 class="text-white mb-3">@item.SubTitle</h3>
                <a href="" class="btn btn-primary">Alışverişe Başlayın</a>
            </div>
        </div>
        @if (count == 2) break; //html görünüm 2 taneye izin veriyor eski tekliflerin silinmesi lazım ilk 2 tanesini ekler sadece
    }
</div>